{
  "name": "donasaurs",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @donasaurs/api dev",
    "dev:web": "pnpm --filter @donasaurs/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
